enum RequestType { get, post, put, delete }

enum NavigationBarItems {
  dashboard,
  profile,
}

enum RouteScreens {
  root,
  splash,
  login,
  baseModule,
  homeScreen,
  details,
}

enum PaginableListViewLastItem {
  progressIndicator,
  errorIndicator,
  emptyContainer
}
